# V0.2 QA 记录

日期：2026-09-12。状态：Beta / 工具链验证通过，实际引擎验收待完成。

## 已执行

Node 22.16.0 / TypeScript 5.8.3：`npm test`，33 项测试全部通过，0 失败。覆盖工程图片持久化、旧工程拒绝、矩阵逆变换、父骨变换、镜像和非中心 pivot 绑定保持位置、骨骼重挂/删除、循环检查、关键帧排序和插值、独立复算导出后的 Y-up 四角、实际 region 尺寸、图集去重/边界、CRC、ZIP 路径与损坏检查、异常输入拒绝。

浏览器：Playwright + 系统 Chromium，离线加载构建后的单文件 HTML，页面未访问网络。7 组端到端检查全部通过，未捕获 JavaScript 页面异常：

1. 内置练习角色打开，13 零件、9 骨骼，工程保存含图片。
2. 动画播放使时间轴前进。
3. 数字修改、撤销/重做、绑定选择能够保存还原。
4. `.project.json` 打开后的序列化结果与保存前完全一致。
5. Spine ZIP、透明 PNG、30 帧 PNG 序列成功下载；Spine ZIP 可以重新打开为工程。
6. 真实鼠标点选画出两节相连骨链，父子关系正确。
7. 390×844 移动视口：导入图片、切换属性/画板、单指触摸拖动、保存；无水平溢出。

独立 Python zipfile/Pillow 检查：ZIP CRC 正确；示例包 22 文件、13 region、1 页图集；PNG 为 RGBA，Alpha 范围 0～255；图集尺寸与 metadata 一致；所有 attachment 路径均对应存在的 region，独立图片尺寸正确；接入示例 import/export 文本未被构建器破坏；序列帧 30 张、透明通道存在。

## 修复的问题

- V0.1 保存时丢弃 src，无法还原图片；本版内嵌完整图片。
- V0.1 Spine 字段是自定义版本且贴图尺寸为 0；本版使用具体目标格式和实际尺寸。
- 导出坐标舍入误差影响独立复算；调整精度后四角复算通过。
- 单文件构建器原型用正则处理模块，会匹配接入示例字符串；改为 TypeScript AST 检查和模块转译。
- 适合画板缩放数值不在下拉选项时显示空白；改为动态显示实际百分比。

## 没有执行，不能宣称通过

- Laya/Cocos SDK 编译、资源导入、Runtime 加载或游戏播放。
- Spine 官方编辑器导入或官方 Runtime 解析。
- iOS Safari/Android 真机、原生平台、小游戏平台。
- HTTP 页面加载和 IndexedDB 跨刷新持久化（测试采用离线页面注入，存储失败会提示手动保存）。
- 大规模复杂角色性能与完整压力/模糊测试。

源码包含 `tests/core.test.mjs` 和可选 `tests/browser_smoke.py`；后者输出截图、资源包和 browser-results.json。不要把这份工具测试记录改称引擎认证。
